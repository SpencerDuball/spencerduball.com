{
  "name": "scripts",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup-env": "yarn run ts-node setup-env --region us-east-1 --env dev"
  },
  "dependencies": {
    "@aws-sdk/client-ssm": "^3.121.0",
    "arg": "^5.0.2",
    "ts-node": "^10.8.2"
  }
}
