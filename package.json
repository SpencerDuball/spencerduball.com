{
  "name": "spencerduballcom",
  "version": "1.0.0",
  "type": "module",
  "keywords": [],
  "author": "Spencer Duball",
  "license": "ISC",
  "scripts": {
    "prod:build": "docker compose -f compose.prod.yaml --env-file .env.production build",
    "prod:run": "docker compose -f compose.prod.yaml --env-file .env.production up -d",
    "prod:stop": "docker compose -f compose.prod.yaml --env-file .env.production down"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
