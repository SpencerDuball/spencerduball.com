{
  "name": "spencerduballcom",
  "version": "1.0.0",
  "type": "module",
  "keywords": [],
  "author": "Spencer Duball",
  "license": "ISC",
  "scripts": {
    "docker-start-prod": "docker compose -f compose.yaml --env-file .env.prod up -d",
    "docker-stop-prod": "docker compose -f compose.yaml --env-file .env.prod down",
    "docker-start-dev": "docker compose -f compose.dev.yaml up -d",
    "docker-stop-dev": "docker compose -f compose.dev.yaml down"
  },
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
