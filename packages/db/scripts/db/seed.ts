import ora from "ora";

/**
 * Resets seed data and then performs all seed actions from the "seed/" folder. This function will
 * only seed the data corresponding to each migration that has been applied.
 */
async function main() {}
